include ../_mixins-links.pug
block variables

doctype html
html(lang=language)
    head
        include ./_head.pug
    body(class=(pageClass != "") ? pageClass : "")
        block content

        include ./_foot.pug